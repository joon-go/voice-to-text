{
  "name": "voice-to-text",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Enterprise Elite first-response responder — page, hear the problem, dictate an original first response, post it (attributed to the live engineer) inside the 15-min SLA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test api/_pylon.test.js"
  },
  "dependencies": {
    "lucide-react": "^0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.1"
  }
}
